import 'package:flutter/material.dart';

class ConfirmePhoneNumber extends StatelessWidget {
  const ConfirmePhoneNumber({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
